import { Client } from 'pg';

/*
const database = new Client({
  user: 'sa',
  host: 'localhost',
  database: 'public',
  password: '',
  port: 9092,
});
export default async function selectIntroduction() {
  try {
    await database.connect();
    console.log('connected');
    const res = await database.query('SELECT * FROM test ');
    console.log(res);
  } catch (err) {
    console.log(err);
  } finally {
    await database.end();
  }
}
*/
